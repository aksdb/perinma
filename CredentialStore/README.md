# CredentialStore

This is a partial copy & paste of the Git Credential Manager.
See https://github.com/git-ecosystem/git-credential-manager

Unused stuff was stripped down as much as possible to only leave behind
the supported backing store implementations. Kudos to the authors of the
git credential manager.
